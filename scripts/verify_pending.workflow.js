export const meta = {
  name: 'openrouter-verify-pending',
  description: 'Adversarially verify the impacted-findings rows still flagged impact_verified:false',
  phases: [{ title: 'Verify', detail: 'one agent per pending repo; re-check every cited figure against source' }],
}

const PENDING = [{"title": "Nous Research — Autoreason (self-refinement with AI-judge panels)", "repo_url": "https://github.com/NousResearch/autoreason", "paper_url": "", "code_url": "https://github.com/NousResearch/autoreason/blob/main/experiments/v2/run_cross_model_judges.py#L40-L44", "venue": "self-published research report (GitHub), Nous Research", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M4", "M5", "M6", "M8"], "impacted_findings": [{"finding": "Table 13 'Four-Tier Comparison': Llama 3.1 8B row -- AR wins 1/3 tasks, AR avg 23.7 vs best baseline 25.0 (single-pass), margin -1.3. This is the weak-model anchor of the headline 'advantage peaks at mid-tier, diminishes at both extremes' claim stated in the abstract and repeated in the README/paper Sec 7.2.", "locator": "paper/autoreason.pdf Table 13 (Sec 7.2 'Four-Tier Comparison'); restated in Abstract and Sec 7 intro", "mechanism": "M1 (unpinned quantization) + M3 (probabilistic routing) + M4 (no provenance logging). experiments/v2/run_capability_curve.py:46 sets llama8b = 'openrouter/meta-llama/llama-3.1-8b-instruct' with no provider/quantizations/require_parameters and no logged generation id (confirmed by direct grep of the file -- no extra_body, provider, quantizations, or generation_id anywhere). Llama-3.1-8B-Instruct is served by many OpenRouter providers at varying precisions (fp16 down to int4); a silently-served degraded quantization would depress or distort the author model's outputs, and the reported margin (-1.3 out of ~25) is small enough that a quantization-driven swing could flip its sign or magnitude -- which is exactly the 'too weak at the bottom' pole the paper leans on for the U-shape narrative.", "severity": "high", "depends_on_or": "high"}, {"finding": "'42/42 perfect sweep' -- Haiku 3.5 + autoreason scored a perfect Borda (42/42) across 3 tasks while every refinement baseline degraded below single-pass (33.7 avg). Quoted in the paper abstract, Table 12, and the README 'Key Results' table; also the Haiku 3.5 row of Table 13 (avg 42.0 vs single 33.7, margin +8.3) and Table 21/Sec 7.11's 'Haiku 3.5: 40% AR private vs 38% single, but +8pp vs best-of-6 (40% vs 31%)' code-domain corroboration.", "locator": "Abstract; README 'Key Results' row 1; paper Table 12, Table 13 (Haiku 3.5 row), Table 21/Sec 7.11 (Haiku 3.5 tier), Sec 7.9 Table 19 ('40% vs 31%')", "mechanism": "M2 (silent param dropping) + M4 (no provenance) + M6 (version drift). Both the writing-task and code-task Haiku-3.5 generation calls use 'openrouter/anthropic/claude-3.5-haiku' -- an UNDATED alias (contrast with the directly-called anthropic/claude-sonnet-4-20250514, which is dated) in run_small_vs_big.py:54, run_code_overnight.py, and run_haiku_code.py, with no extra_body/provider pin and no generation id logged. Because Anthropic is the sole upstream provider for Claude models on OpenRouter, third-party quantization risk (M1) is low, but silent snapshot drift under the undated alias and undetected sampling-param handling differences between OpenRouter's endpoint and Anthropic's direct endpoint cannot be ruled out or audited after the fact -- and this is the single most-quoted number in the whole report.", "severity": "high", "depends_on_or": "medium"}, {"finding": "Table 13 Gemini Flash row (AR wins 2/3, avg 25.0 vs single 20.0, margin +5.0) and Table 21/Sec 7.11 code-scaling-curve use of Gemini -- more importantly, the paper's own Appendix D ('Reproducibility Details') states scaling experiments used 'gemini-2.0-flash via Google AI' (i.e., direct, not OpenRouter), but the actual source code contradicts this.", "locator": "paper Table 13 (Gemini Flash row) and paper Appendix D reproducibility list vs. experiments/v2/run_capability_curve.py:45 and experiments/v2/run_small_vs_big.py:53", "mechanism": "M4 (no provenance logging) surfaced as a direct documentation/code contradiction: run_capability_curve.py and run_small_vs_big.py both hardcode gemini_flash = 'openrouter/google/gemini-2.0-flash-001', not a direct Google AI call as Appendix D claims. This means the paper's own disclosed reproducibility methodology is wrong for this tier, undermining confidence that other undisclosed OpenRouter usage isn't present elsewhere, and leaves the Gemini Flash results with the same unpinned-routing exposure (M2/M3) as the other OR-routed tiers, just with lower quantization risk since Gemini is a proprietary, primarily-single-served model.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Table 21 'Code Scaling Curve' Haiku 3.5 point (40% AR private-test vs 38% single, delta reported as 'No (but +8pp vs best-of-6)') and the compute-matched headline '40% vs 31%' (Haiku 3.5 autoreason vs best-of-6 sampling, Table 19) -- the only OR-routed rung of an otherwise direct-Anthropic 4-point curve (Haiku 4.5 60%, Sonnet 4 64%, Sonnet 4.6 77% all use anthropic/ prefixes called directly, confirmed via run_code_haiku45.py and run_code_overnight.py).", "locator": "paper Table 21 (Sec 7.11) and Table 19 (Sec 7.9)", "mechanism": "M3 (probabilistic routing) + M8 (cross-provider/run comparison confound). Both arms of the '40% vs 31%' compute-matched comparison (autoreason vs best-of-6) share the same unpinned model string 'openrouter/anthropic/claude-3.5-haiku' but were run as separate script invocations (run_code_overnight.py supports partial problem-range runs, i.e., 'overnight' batches) that could each be routed to a different served snapshot/backend by OpenRouter at different times, silently biasing the internal comparison the headline number depends on.", "severity": "medium", "depends_on_or": "medium"}, {"finding": "Cross-model judge robustness check (GPT-4o and Gemini-2.5-Flash-preview as alternate judges, meant to test whether the Borda results are an artifact of same-model judge/author preference bias) -- referenced only qualitatively in the paper text ('our same-model judges violate the independence assumption... we do not claim game-theoretic guarantees') with no specific published number, and the repo's own saved results only contain claude_sonnet4 and gpt4o subdirectories for tasks 1 and 4 -- no gemini_flash results were saved, consistent with a silently failed/discarded run.", "locator": "experiments/v2/run_cross_model_judges.py (the task's flagged primary OpenRouter call site, L40-44) and experiments/v2/results_cross_model_judges/ (missing gemini_flash subdirectory)", "mechanism": "M9 (judge on unconstrained route) + M2. Judges must emit a free-text 'RANKING: X, Y, Z' line for the panel to parse; per the paper's own Appendix D, 'If parsing fails, the judge response is discarded and not retried.' JUDGE_MODELS routes gpt4o and gemini_flash through 'openrouter/openai/gpt-4o' and 'openrouter/google/gemini-2.5-flash-preview' with no structured-output/require_parameters constraint and no provider pin, so a provider that mishandles the free-form instruction could silently drop judge votes rather than erroring -- plausibly explaining why no gemini_flash results were ever saved, and undermining the one mechanism the paper offers to rule out same-model judge bias for the headline Borda numbers.", "severity": "low", "depends_on_or": "medium"}], "headline_impact": "The abstract/README headline claim that autoreason's advantage forms a U-shape \"peaking at mid-tier and diminishing at both extremes\" (Table 13, Four-Tier Comparison) rests on 3 of its 4 model tiers (Llama 3.1 8B, Gemini Flash, Haiku 3.5) being generated through unpinned, unlogged OpenRouter routing -- most critically the weak-end anchor, Llama-3.1-8B-Instruct, an open-weight model with a -1.3 Borda margin that many OpenRouter providers serve at reduced (down to int4) quantization.", "no_impact_reason": ""}, {"title": "Prompt Framing Changes LLM Performance (and Safety) (kmerkelbach/llm-request-tone)", "repo_url": "https://github.com/kmerkelbach/llm-request-tone", "paper_url": "https://www.lesswrong.com/posts/RTHdQuGJeBKWHbgyj/prompt-framing-changes-llm-performance-and-safety", "code_url": "https://github.com/kmerkelbach/llm-request-tone/blob/main/src/evaluation/config.py#L34-L41", "venue": "LessWrong", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M4", "M5", "M6", "M8"], "impacted_findings": [{"finding": "\"Expert framing was shown using statistical testing to increase the rate of compliance with harmful requests\" (repo README summary and LessWrong post headline claim), with statistical significance reported at p=0.0024, and per-family SORRY-Bench compliance deltas for the Expert scenario of +18.1% (Llama) and +41.9% (Qwen) in tables/sorry_bench/mean/scenario_vs_family.md (LessWrong post prose rounds these to +18.5% and +40.5%).", "locator": "README.md \"Results > Summary\"; LessWrong post body; tables/sorry_bench/mean/scenario_vs_family.md (Expert row)", "mechanism": "M1 (no quantization pin) + M3 (probabilistic routing) + M9 (judge on unconstrained route): src/evaluation/config.py L34-41 lists bare OpenRouter slugs (openai/gpt-oss-20b, openai/gpt-oss-120b, qwen/qwen-2.5-7b-instruct, qwen/qwen-2.5-72b-instruct, meta-llama/llama-3.1-8b-instruct, meta-llama/llama-3.1-70b-instruct) with zero provider/quantization/order constraints, confirmed in src/evaluation/sorry_bench_shell.py which passes `model` and `judge_model=\"openai/gpt-oss-120b\"` straight to the forked SORRY-Bench's gen_api_answer.py / gen_judgment_safety.py scripts (README confirms this fork was \"modified to work with OpenRouter\") with no provider pinning found anywhere in either repo. Each of the 16 repetitions per (model, scenario) can be silently served by a different provider at a different quantization level, and the judge that converts free-text answers into a binary compliance/refusal score can likewise land on a degraded/quantized route each call, directly perturbing the compliance-rate numerator that the Welch's t-test + Bonferroni correction treats as clean i.i.d. draws from one fixed model.", "severity": "high", "depends_on_or": "high"}, {"finding": "MBPP+ (\"Python coding\") shows by far the largest framing effects of any capability benchmark: Expert +56.4%, Reciprocity +69.3%, Flattery +56.7%, Grandma +52.9%, Breathing +33.0% (tables/lm-eval/mean/scenario_vs_benchmark.md), versus single-digit-percent or negative deltas for GPQA Diamond and TruthfulQA in the same table; LessWrong post further quotes \"Flattery and threats improved Python coding by +3.6% and +2.6%\" (a different aggregation) and states coding \"profits most strongly from framing.\"", "locator": "tables/lm-eval/mean/scenario_vs_benchmark.md (MBPP+ column); LessWrong post body", "mechanism": "M1 + M8: all six models are run through lm-eval-harness's `local-chat-completions` backend pointed at `base_url=\"https://openrouter.ai/api/v1/chat/completions\"` (src/evaluation/lm_eval_shell.py L34-41), with model_args containing only model/base_url/api_key/num_concurrent/max_retries — no provider or quantization field. MBPP+ correctness is a hard pass/fail code-execution metric, which is unusually sensitive to quantization-induced degradation (int4 vs fp16 can flip exact-match code correctness far more than it shifts multiple-choice accuracy on GPQA/TruthfulQA), making the outsized MBPP+ deltas plausibly explainable by run-to-run provider/quantization drift (M1) compounded by cross-model/cross-run provider mixing (M8) rather than a genuine framing effect on coding ability.", "severity": "medium", "depends_on_or": "high"}, {"finding": "\"Large models comply more often with harmful requests than small models\" (p-value: 1.6773e-07), i.e., a systematic small-vs-large comparison across gpt-oss-20b/120b, Qwen 2.5 7B/72B, and Llama 3.1 8B/70B.", "locator": "LessWrong post body (safety findings section)", "mechanism": "M8 (cross-provider comparison confound): the \"small\" and \"large\" variants of each family are different OpenRouter model slugs and may be concentrated on different provider pools with different default quantization/precision (e.g., smaller open-weight models are more commonly served at aggressive quantization by more providers than their larger counterparts). Because config.py pins no provider/quantization for any of the six slugs, part of the apparent size-driven compliance gap could reflect systematic small-vs-large differences in which OpenRouter providers/quantizations actually served each size tier, not model-scale-driven safety-tuning differences.", "severity": "medium", "depends_on_or": "high"}, {"finding": "The reported per-benchmark relative-standard-error range \"2.8%-17.7%\" (tables/stderr_analysis.md) used to justify statistical power for the Welch's t-test/Bonferroni-corrected significance claims across all lm-eval and SORRY-Bench tables (tables/lm-eval/* and tables/sorry_bench/*), computed from 16 repetitions per (model, scenario, benchmark) combination at temperature=0.7.", "locator": "tables/stderr_analysis.md; underlying tables/lm-eval/* and tables/sorry_bench/* CSV/MD tables", "mechanism": "M1 + M3 + M4 (no provenance logging): the 16 repetitions per cell are drawn via repeated OpenRouter calls to the bare model slug with no provider pinning and no logging of which provider/quantization actually served each call (nothing in lm_eval_shell.py, sorry_bench_shell.py, or dto.py records provider identity). The measured within-cell variance (feeding directly into the stderr estimates and hence the t-tests' significance thresholds) therefore conflates genuine sampling variance at temperature 0.7 with variance from silent provider/quantization switching across repetitions, and there is no way after the fact to separate the two because provenance was never captured.", "severity": "medium", "depends_on_or": "high"}], "headline_impact": "The paper's central claim — \"Expert framing was shown using statistical testing to increase the rate of compliance with harmful requests\" (p=0.0024, with SORRY-Bench compliance-rate deltas of roughly +18-42% for Expert framing per model family) — is computed entirely from OpenRouter calls to bare open-weight model slugs (no provider/quantization pin anywhere in the codebase) AND judged by gpt-oss-120b routed the same unconstrained way, so quantization/provider drift across the 16 repetitions per condition is a fully plausible confound for the reported effect.", "no_impact_reason": ""}, {"title": "Seer (interpretability agent harness) / chinese-models-investigations (blackbox de-censoring experiments)", "repo_url": "https://github.com/ajobi-uhc/seer", "paper_url": "https://www.alignmentforum.org/posts/7gp76q4rWLFi6sFqm/test-your-interpretability-techniques-by-de-censoring-1", "code_url": "https://github.com/ajobi-uhc/seer", "venue": "Alignment Forum", "uses_safely": false, "mistake_ids": ["M2", "M4", "M5"], "impacted_findings": [{"finding": "Correctness Score panel of 'Evaluation Results (Multiple Runs)' (agent-runs/evaluation_results.png, embedded in/summarized by the AF post): per-task means across n=5 runs — 'China Elicit Qwen' ≈6.8, 'Quote Harry Potter' ≈6.4, 'Kimi Investigation' ≈-6.8 (the lowest, called out in the post as the hardest task), 'Kimi Investigation Attacker' ≈2.5", "locator": "chinese-models-investigations/agent-runs/evaluation_results.png (top subplot 'Correctness Score'); same chart appears per-run at agent-runs/run{1-5}/evaluation_results.png", "mechanism": "M2 (silent param/behavior dropping) + M4 (no provenance logging). These 4 columns target open-weight models exclusively via OpenRouter (qwen/qwen3-vl-30b-a3b-instruct via provider 'siliconflow/fp8'; moonshotai/kimi-k2-0905 via 'siliconflow/fp8'; openai/gpt-oss-20b via 'inference-net' — confirmed from falun-gong-qwen.yaml, kimi-incident.yaml, kimi-attacker.yaml, harry-potter-quote.yaml). The mandated elicitation method (system_prompt.md's 'prefill attack'/'CoT prefilling attack', used in nearly all 85 chat.completions.create() call-sites) relies on the serving engine genuinely continuing generation from a trailing assistant-role message rather than starting a fresh turn — an OpenAI-compatible-API behavior that differs by inference engine and provider, is never confirmed, and would fail silently (no error, no fallback since allow_fallbacks=False just means 'error if the pinned provider itself is down', not 'error if it mishandles the request shape'). The query_model()/cot_prefill_attack() functions (verified in run1 notebooks) pass temperature but capture only response.choices[0].message.content — no OpenRouter-returned provider/model/version metadata is logged anywhere across the 85 call-sites, so post-hoc it is impossible to confirm every call was served with the exact sampling params and continuation semantics the researchers assumed. Because the scored metric IS 'did the elicitation succeed,' a silent per-provider prefill/param quirk would directly move these correctness numbers and could masquerade as genuine task-difficulty variation (the post's own explanation for the Qwen/Kimi gap).", "severity": "medium", "depends_on_or": "high"}, {"finding": "Bayesian Score panel of the same figure: e.g. 'Kimi Investigation' mean ≈-7.6 (worst), 'Kimi Investigation Attacker' mean ≈5.0, 'Quote Harry Potter' mean ≈-2.5 — used to argue the agent's reasoning-quality/belief-updating differs sharply by task", "locator": "chinese-models-investigations/agent-runs/evaluation_results.png (bottom subplot 'Bayesian Score')", "mechanism": "Same M2+M4 mechanism as above, one step removed: the consistency/Bayesian judge (eval/consistency.md, run via direct Anthropic API — not OpenRouter, so the judge itself is unaffected) scores the agent's reasoning based on the evidence the target model actually produced in the notebook. If a provider-level prefill/param quirk silently altered what the OpenRouter-routed target model returned, the judge is scoring reasoning over a corrupted evidence stream, and there is no logged record to distinguish 'target model genuinely refused/lied' from 'provider silently broke the elicitation technique.'", "severity": "medium", "depends_on_or": "high"}, {"finding": "Qualitative 'successful de-censoring' transcripts referenced in the post's narrative and archived under transcripts-for-blog/ (e.g. zhuhai_1.md/zhuhai_2.md/zhuhai_3.md — Zhuhai vehicular-attack details elicited from Kimi K2; arya_falun_gong.md — Falun Gong persecution details elicited from Qwen3) presented as characteristic examples of what the prefill technique can extract", "locator": "chinese-models-investigations/transcripts-for-blog/*.md, referenced qualitatively in the AF post", "mechanism": "M4 primarily: with zero OpenRouter response-metadata logging across all 85 call-sites, there is no way to confirm these particular 'successful' transcripts were generated under the same serving conditions (engine, effective quantization at the siliconflow/fp8 endpoint) as the majority of less-successful runs on the same task/model. Their use as illustrative, representative examples of the technique's power can't be distinguished from a favorable one-off provider/engine quirk versus a reproducible property of the method.", "severity": "low", "depends_on_or": "medium"}], "headline_impact": "The headline evaluation_results.png figure (correctness + Bayesian consistency scores, n=5 runs per task) that anchors the post's \"Kimi investigation is the hardest de-censoring task\" narrative is built entirely from OpenRouter-routed calls to open-weight models (Qwen3, Kimi K2, gpt-oss-20b) whose primary elicitation technique — assistant-message prefill/CoT-prefill attacks — depends on undocumented per-provider handling of trailing-assistant continuation and sampling params that the code never verifies or logs (M2 + M4); the fifth column in that same figure, \"User Preference,\" is NOT affected — it targets a separately fine-tuned model served locally via Modal/HuggingFace `model.generate()`, with no OpenRouter involvement at all.", "no_impact_reason": ""}, {"title": "ctfish (Palisade Research — \"Demonstrating Specification Gaming in Reasoning Models\")", "repo_url": "https://github.com/PalisadeResearch/ctfish", "paper_url": "https://arxiv.org/abs/2502.13295", "code_url": "https://github.com/PalisadeResearch/ctfish/blob/main/scoring/score_stages.py#L14", "venue": "arXiv (ICML-format writeup); widely covered by Time/Fortune/Business Insider and heavily cited in AI-control/scheming discourse", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M4", "M5", "M6", "M8", "M10"], "impacted_findings": [{"finding": "Abstract/headline claim \"DeepSeek R1 will often hack the benchmark by default\"; Figure 2 \"Baseline runs summary\" DeepSeek R1 row (environment_failure 0.15, replace_stockfish 0.04, replace_board 0.08, n=52); Figure 8 step-count histogram; Appendix F.3 Figures 15-17; §8 Discussion table classifying DeepSeek R1 as a hacking \"1st gen Reasoning Model\"", "locator": "Abstract; Introduction bullet 1 (p.1); §8 Discussion \"Model Type\" table; Figure 2; Figure 8 \"Distribution of number of steps\"; Appendix F \"Hacking breakdown\" + F.3 (Figures 15-17)", "mechanism": "M3 probabilistic routing + M1 unpinned quantization + M11 silent backend mixing, compounded by M4 no provenance logging. deepseek/deepseek-r1 is invoked via OpenRouter in player.py's Provider class with zero provider pinning anywhere in the repo (no `provider` dict / extra_body). The authors' own Appendix F states: \"The DeepSeek agents have run for fewer steps because the inference API was unstable and we had trouble recording long runs... this underestimates DeepSeek's hacking success because it has fewer steps to work with,\" and explicitly flags DeepSeek R1's breakdown as \"not directly comparable\" to o1-preview's. Figure 8 shows R1's session-length distribution concentrated near 0 steps versus o1-preview's cluster at 15-30 — the classic signature of an unpinned multi-provider route silently bouncing between backends of inconsistent uptime/latency/quantization rather than a property of the model itself. This also invalidates the apples-to-apples model comparison (M8) the abstract's claim rests on, since o1-preview/o3/Claude were served through single, consistent, proprietary APIs while R1 was not.", "severity": "high", "depends_on_or": "high"}, {"finding": "Introduction bullet: \"o1-mini, o3-mini, and QwQ 32B Preview can't use the game environment effectively\"; §4.5: \"QwQ 32B Preview... wasn't able to follow the agent instructions properly\"; Figure 2 / Figure 18 QwQ 32B Preview environment_failure = 1.00 (baseline, n=49) and 0.98 (spooky, n=48), with 0.00 on every other category", "locator": "Introduction (p.1, 3rd bullet); §4.5 \"Other language models\"; Figure 2 \"Baseline runs summary\"; Figure 18 \"Unexpected behavior heatmap for all models\" (Appendix G)", "mechanism": "M1 unpinned quantization + M2 silent param dropping + M3 probabilistic routing. qwen/qwq-32b-preview is a bare, undated open-weight slug in player.py's OPENROUTER_MODELS set with no `provider` dict anywhere in the repo, so each call could land on any OpenRouter backend at any quantization (down to int4), and any backend could silently strip/alter the harness's default temperature=0.5 or mishandle its XML-tag-structured prompting. A near-total (100%/98%) environment_failure classification is exactly the failure signature a degraded/quantized or param-stripped backend would produce (malformed <ACTION>/<PLAN> tags the BeautifulSoup parser can't find). Unlike DeepSeek R1, the paper does not discuss API instability for QwQ and offers no per-call provenance to rule this out, so this is a plausible but author-unconfirmed confound rather than a demonstrated one — the 32B model's genuine incapability is also a real possibility.", "severity": "medium", "depends_on_or": "medium"}], "headline_impact": "The abstract's headline claim \"reasoning models like OpenAI o3 and DeepSeek R1 will often hack the benchmark by default\" leans on DeepSeek R1 hacking-propensity numbers (Fig. 2/8/15-17) that the paper's own Appendix F admits are \"not directly comparable\" to other models and an underestimate, because deepseek-r1 rollouts went through unpinned OpenRouter routing whose \"inference API was unstable.\" Correction to the task's premise: the actual OR-exposed call site is player.py's Provider.invoke() (agent rollouts for deepseek/deepseek-chat, deepseek/deepseek-r1, qwen/qwq-32b-preview, google/gemini-2.0-flash-thinking-exp:free, none pinned via `provider`/`extra_body`); scoring/score_stages.py's LLM judge uses a plain `AsyncOpenAI()` client (gpt-4o-mini) hitting OpenAI directly, not OpenRouter, so M9 (judge on unconstrained route) does not apply here.", "no_impact_reason": ""}, {"title": "diffing-toolkit (science-of-finetuning / Neel Nanda, Julian Minder, Clément Dumas, Stewy Slocum — MATS 7)", "repo_url": "https://github.com/science-of-finetuning/diffing-toolkit", "paper_url": "https://www.alignmentforum.org/posts/sBSjEBykQkmSfqrwt/narrow-finetuning-leaves-clearly-readable-traces-in", "code_url": "https://github.com/science-of-finetuning/diffing-toolkit/blob/main/configs/diffing/evaluation.yaml#L17", "venue": "Alignment Forum", "uses_safely": false, "mistake_ids": ["M4", "M5", "M6"], "impacted_findings": [{"finding": "Agent Performance Comparison (headline): ADL agent, 5 interactions: 97% reached score >=2, 60% reached >=4; ADL agent, 0 interactions: 94% >=2, 39% >=4; Blackbox baseline, 50 interactions: 12% >=2, 6% >=4 (1-5 hypothesis-grading rubric)", "locator": "AF post 'Headline Results' section (Agent Performance Comparison); generated by narrow_ft_experiments/plotting/plot_agent_performance.py (variants agent_mi0/agent_mi5/baseline_mi0/mi5/mi50) from data graded via configs/diffing/evaluation.yaml", "mechanism": "M6 (version drift) + M4 (no provenance logging). configs/diffing/evaluation.yaml pins agent.llm.model_id to the unversioned OpenRouter alias 'openai/gpt-5' (a commented-out alternative 'anthropic/claude-sonnet-4' shows the field was manually swapped at times) and grader.model_id to 'openai/gpt-5-mini' -- neither is a dated/pinned snapshot. Git history on that file shows active edits from 2025-11-04 through 2026-02-05 (interaction budgets changed, baselines toggled on/off, grader runs added), so the ADL and blackbox-baseline conditions being compared were very plausibly produced in separate sessions spread over months. Neither src/diffing/utils/agents/llm.py (AgentLLM.chat) nor src/diffing/utils/graders/grader.py (Grader._call_with_retry) records the actual served model id / provider metadata returned by OpenRouter (e.g. completion.model, system_fingerprint) -- only response text and token usage are kept. If OpenAI silently rolled the default 'gpt-5'/'gpt-5-mini' snapshot between when the ADL condition and the blackbox-baseline condition were run, the method-vs-baseline gap would be confounded with a model-version difference, and this is now unauditable.", "severity": "high", "depends_on_or": "high"}, {"finding": "Data-mixing mitigation ablation: mixing pretraining samples 1:1 with finetuning data drops the agent's hypothesis-grading score to 1 (near-failure) versus 3 on unmixed data", "locator": "AF post 'Mitigation Through Training Data' section", "mechanism": "M6 + M4, same as above: this ablation runs through the identical agent (openai/gpt-5) + HypothesisGrader (openai/gpt-5-mini) OpenRouter pipeline defined in configs/diffing/evaluation.yaml, and inherits the same unpinned-alias / no-provenance-logging exposure. Given the pipeline's grading rubrics and evaluation config were edited repeatedly across the Nov 2025-Feb 2026 window (per commit history), the mixed and unmixed conditions were likely run in different sessions, so a mid-collection snapshot change on either 'gpt-5' or 'gpt-5-mini' could shift the 1-vs-3 gap without being detectable from stored logs.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Patchscope Token Relevance: 'the maximum fraction of relevant tokens in top-20 Patchscope outputs [is] significantly higher than the baselines' across all organism (finetune) types", "locator": "AF post 'Headline Findings' -- Patchscope Token Relevance claim", "mechanism": "M6 + M4: TokenRelevanceGrader is configured in configs/diffing/method/activation_difference_lens.yaml with grader.model_id: openai/gpt-5-mini, base_url https://openrouter.ai/api/v1 -- again an unpinned alias, and the same Grader base class (src/diffing/utils/graders/grader.py) discards any served-model/provider metadata from the OpenRouter response. This claim aggregates across many organism conditions (see the ~80 configs/organism/*.yaml files) almost certainly graded at different times over the multi-month development window, so any silent 'gpt-5-mini' snapshot change would unevenly affect the per-organism relevance scores being compared.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Grader cross-model agreement / reliability statistics (Pearson correlation per grader pair and Krippendorff's ordinal alpha) computed between GPT-5-mini, Gemini 2.5 Flash, and Claude Haiku 4.5 as judges on the same hypothesis samples", "locator": "narrow_ft_experiments/hibayes/agent_grades/grader_agreement.py, producing tab:grader_agreement (LaTeX table exported to grader_agreement.tex) -- a judge-reliability/robustness check likely underlying reported inter-grader agreement", "mechanism": "M6 + M4: all three grader backends (openai/gpt-5-mini, google/gemini-2.5-flash, anthropic/claude-haiku-4.5) are called through the identical unpinned-alias, provenance-blind OpenRouter path in src/diffing/utils/graders/grader.py. This analysis exists specifically to establish that the judge is reliable/model-agnostic, but if any of the three vendors silently updated their default snapshot mid-collection, the reported correlation/alpha would reflect a partly-nonstationary rater rather than three fixed raters -- undermining the very robustness claim the statistic is meant to support, in an unknown direction.", "severity": "low", "depends_on_or": "high"}], "headline_impact": "The post's central claim — that the mech-interp agent (ADL, using an OpenRouter-routed openai/gpt-5 hypothesis-generator + openai/gpt-5-mini judge) dramatically outperforms a blackbox baseline (e.g. 97%/60% score≥2/≥4 at 5 interactions vs 12%/6% for the 50-interaction blackbox baseline) — was produced by unpinned OpenRouter model aliases with no response-side provenance logging, across a pipeline whose configs were actively edited over a multi-month window, so a silent vendor-side snapshot change between the ADL and baseline runs cannot be ruled out or even detected after the fact.", "no_impact_reason": ""}, {"title": "lighteval — Swiss-Legal / LEXam LLM-as-judge tasks (JudgeLM litellm backend)", "repo_url": "https://github.com/huggingface/lighteval", "paper_url": "https://arxiv.org/abs/2505.12864", "code_url": "https://github.com/huggingface/lighteval/blob/main/src/lighteval/metrics/utils/llm_as_judge.py#L333-L339", "venue": "ICLR 2026 (LEXam: Benchmarking Legal Reasoning on 340 Law Exams)", "uses_safely": false, "mistake_ids": ["M1", "M3", "M4", "M5", "M6", "M8", "M10", "M11"], "impacted_findings": [{"finding": "SLDS (Swiss Landmark Decision Summarization / headnote-generation) judge score, metric `slds_judge_deepseek_v3` — computed for all 9 decision-language × headnote-language task variants (de/fr/it × de/fr/it) and used unconditionally as the primary quality judge for every generated headnote", "locator": "src/lighteval/tasks/multilingual/tasks/swiss_legal/metrics.py, get_swiss_landmark_decision_summarization_judge() lines 793-815 (default `model_name=\"openrouter/deepseek/deepseek-chat\"`, line 795), invoked with no override from HeadnoteGenerationTask._get_metrics() in swiss_legal/main.py", "mechanism": "M1+M3+M11: the shared JudgeLM.__call_litellm kwargs (llm_as_judge.py lines 333-339: model/messages/n/caching/response_format only, no `provider` key) mean OpenRouter free-load-balances this open-weight DeepSeek-V3 MoE model across whichever third-party providers list it, at whatever quantization they choose (down to int4), and can switch provider between runs or even between concurrent requests (ThreadPoolExecutor, 10 concurrent). Because scoring depends on the judge reliably emitting 5 exact `*_SCORE: N` rubric lines (regex-parsed in JudgeSwissLandmarkDecisionSummarization._process_judge_response), a degraded/quantized provider silently produces fewer parseable lines ('Could only extract N out of 5 scores' warning path) and deflated scores. M6: the OpenRouter alias `deepseek/deepseek-chat` has historically been repointed to different underlying DeepSeek checkpoints over time, so the same hardcoded string is not a fixed model across reproduction attempts. M5+M4+M10: Swiss federal court decision text is sent through OpenRouter with no `data_collection:\"deny\"` pin, and no provenance is logged, so nobody can audit after the fact which provider/quantization graded which run.", "severity": "high", "depends_on_or": "high"}, {"finding": "Swiss Legal Translation (SLT) judge-ensemble metrics for the Llama-3.3-70B-Instruct judge — `slt_judge_llama_3_3_70b_{basic,detailed,codebook}_{diverse,single}_*` (6 of the 24 ensemble metric variants), computed for every translation task variant across the sdst/slt/sscprt dataset families (many language pairs)", "locator": "metrics.py: JUDGE_MODELS[\"llama-3-3-70b\"] = \"openrouter/meta-llama/llama-3.3-70b-instruct\" (line 932) feeds JUDGE_METRICS (lines 967-978), which is unconditionally selected into METRICS_TO_USE because `metrics_to_evaluate = [\"judge\"]` is hardcoded (line 980) — TranslationTask has no toggle to exclude it", "mechanism": "M1+M3+M11: same unpinned-provider/quantization exposure as above, but for an open-weight Llama-3.3-70B judge scoring translation quality. M8: any comparison of candidate-model translation quality that relies on this judge is confounded because the judge's own output quality drifts run-to-run with provider/quantization. M5+M4+M10: legal-translation text routed through OpenRouter with no data-policy pin and no provenance logging.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Swiss Legal Translation (SLT) judge-ensemble metrics for the OpenAI-family judges (o1, gpt-4o, gpt-4o-mini), all string-prefixed `openrouter/openai/...` (18 of the 24 ensemble metric variants, same task scope as above)", "locator": "metrics.py JUDGE_MODELS dict, lines 928-942", "mechanism": "M5+M4: OpenAI is effectively the sole fulfilling backend for its own models on OpenRouter, so quantization risk (M1) is low, but the calls still route through OpenRouter as an undeclared third-party processor with no `data_collection:\"deny\"` pin, and no provenance log confirms each call actually reached OpenAI. M6: version-drift risk on aliases like `openai/o1` (comment in code itself notes it was 'Updated to match OpenRouter availability; previously: openai/o1-2024-12-17', i.e. the maintainers already had to hand-patch this once).", "severity": "low", "depends_on_or": "medium"}], "headline_impact": "lighteval's default LLM-judge for its Swiss Landmark Decision Summarization (SLDS) headnote-generation task family is hardcoded to `openrouter/deepseek/deepseek-chat` (open-weight DeepSeek-V3) with no provider pin, so every downstream user/leaderboard run of lighteval's swiss_legal SLDS tasks inherits an unpinned, unlogged, possibly-quantized judge — by contrast, the actual ICLR 2026 LEXam paper's own headline open-question and MCQ scores are NOT on this path (its default judge is a direct, non-OpenRouter OpenAI call, and MCQ scoring uses no LLM judge at all).", "no_impact_reason": ""}, {"title": "safety-tooling (safety-research org shared alignment-research inference library)", "repo_url": "https://github.com/safety-research/safety-tooling", "paper_url": "", "code_url": "https://github.com/safety-research/safety-tooling/blob/main/tests/test_other_apis.py#L73-L100", "venue": "Multiple (shared infra: Anthropic Fellows Program / MATS alignment-research ecosystem)", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M4", "M5", "M8", "M9", "M10", "M11"], "impacted_findings": [{"finding": "The hardcoded OPENROUTER_MODELS set — meta-llama/llama-3.1-405b, meta-llama/llama-3.1-405b:free, meta-llama/llama-3.1-405b-instruct, meta-llama/llama-3.1-70b-instruct, meta-llama/llama-3.1-8b-instruct, meta-llama/llama-3.3-70b-instruct, mistralai/mistral-large-2411, mistralai/mistral-small-24b-instruct-2501, mistral/ministral-8b, deepseek/deepseek-v3-base:free, deepseek/deepseek-r1-zero:free, nousresearch/hermes-3-llama-3.1-70b, nousresearch/hermes-3-llama-3.1-405b — is the library's silent default route to OpenRouter for any downstream repo's model_id argument.", "locator": "safetytooling/apis/inference/openrouter.py L17-33 (OPENROUTER_MODELS set); safetytooling/apis/inference/api.py model_id_to_class(), \"elif model_id in OPENROUTER_MODELS or model_id.startswith('openrouter/'): return self._openrouter\" branch", "mechanism": "M1 (no quantizations filter anywhere in the codebase) + M3 (probabilistic routing). Confirmed by reading the full 399-line openrouter.py: the only kwargs manipulation is converting logprobs->top_logprobs; kwargs are otherwise passed unmodified to aclient.chat.completions.create(**kwargs) with no default provider/quantization constraint. Any run against these open-weight IDs can be served at a different quantization (down to int4) or by a different upstream inference engine on every call, non-reproducibly.", "severity": "high", "depends_on_or": "high"}, {"finding": "Six model IDs (meta-llama/llama-3.1-8b-instruct, meta-llama/llama-3.1-70b-instruct, meta-llama/llama-3.3-70b-instruct, meta-llama/llama-3.1-405b-instruct, nousresearch/hermes-3-llama-3.1-405b, mistralai/mistral-small-24b-instruct-2501) are labeled in a source comment \"# DeepInfra models accessible through OpenRouter\", implying the developers believed these were pinned to a single known upstream provider — but no provider.order/allow_fallbacks parameter is ever set to enforce that.", "locator": "safetytooling/apis/inference/openrouter.py L25-32 (comment above the 6 model IDs in OPENROUTER_MODELS)", "mechanism": "M1 + M11 (silent backend mixing). The comment documents an intent (route to DeepInfra) that the code never enforces, so OpenRouter's default load balancer can route these IDs to any provider currently serving them (Novita, Together, Fireworks, etc.), each potentially at different quantization/precision, invisibly under the same model_id string across runs of the same downstream experiment.", "severity": "medium", "depends_on_or": "high"}, {"finding": "LLMResponse objects returned by OpenRouterChatModel.__call__ hardcode cost=0 and never capture which upstream provider or quantized variant actually served the request, for both the n>1 and n=1 response-construction branches.", "locator": "safetytooling/apis/inference/openrouter.py, LLMResponse(...) constructions inside __call__ (~L340-390); no `provider` field is populated anywhere in the file", "mechanism": "M4 (no provenance logging). Even the fact that a distortion occurred (quantization change, provider switch) is unrecoverable after the fact for any downstream repo, because the wrapper discards the routing metadata OpenRouter would otherwise expose. This forecloses retroactive auditing of every OpenRouter-routed result the library has ever produced.", "severity": "medium", "depends_on_or": "high"}, {"finding": "force_provider=\"openrouter\" — used at the primary call site tests/test_other_apis.py L82-98 (test_openrouter_call, the exact call site cited in this task) and available to every downstream caller — only selects the OpenRouterChatModel *class*; it is not an OpenRouter provider/quantization pin, despite REPO_GUIDE.md L314 describing force_provider generically as a way to \"force use of a specific provider.\"", "locator": "tests/test_other_apis.py L74-98 (test_openrouter_call, force_provider=\"openrouter\"); REPO_GUIDE.md L293/L314 (force_provider documentation)", "mechanism": "M3 + M10. A downstream researcher who copies this pattern to \"pin reproducibility\" is not pinning anything upstream of OpenRouter's own load balancer — the naming makes it easy to mistake provider-class selection (safety-tooling picks OpenRouter's API) for provider/quantization selection (which specific inference host serves the weights), so the mistake goes unreported.", "severity": "medium", "depends_on_or": "high"}, {"finding": "response_format / structured-output kwargs are documented generically as pass-through (\"response_format (for OpenAI)\", REPO_GUIDE.md L316) but OpenRouterChatModel has no special handling, validation, or fallback check for structured-output support when a call is routed to one of the OPENROUTER_MODELS open-weight IDs.", "locator": "safetytooling/apis/inference/openrouter.py __call__ (no response_format-specific code path exists in the file)", "mechanism": "M9 (judge on unconstrained route). If any downstream repo uses one of these OpenRouter-routed model IDs as an LLM judge with a structured-output/JSON-schema constraint, and OpenRouter's router sends that particular call to a provider that ignores response_format, the constraint can be silently dropped server-side with nothing in safety-tooling to detect it.", "severity": "medium", "depends_on_or": "medium"}], "headline_impact": "safety-tooling's default `OpenRouterChatModel` route — auto-selected via `model_id_to_class()` for the ~13 open-weight model IDs hardcoded in `OPENROUTER_MODELS` (e.g. `meta-llama/llama-3.1-405b`, `meta-llama/llama-3.3-70b-instruct`, `mistralai/mistral-large-2411`, `deepseek/deepseek-v3-base:free`, `nousresearch/hermes-3-llama-3.1-405b`) — has no quantization/provider pinning, no provenance logging (`cost=0` is hardcoded, no served-provider field is ever captured), and no filtering of pass-through sampling kwargs anywhere in the ~400-line `openrouter.py`, so every downstream eval/rollout/judge run that calls `InferenceAPI` with one of these model IDs silently inherits non-reproducible, unaudited backend routing.", "no_impact_reason": ""}, {"title": "Evaluating LLMs for accuracy incentivizes hallucinations — experiment code (openai/hallucinations-paper-experiments)", "repo_url": "https://github.com/openai/hallucinations-paper-experiments", "paper_url": "https://doi.org/10.1038/s41586-026-10549-w", "code_url": "https://github.com/openai/hallucinations-paper-experiments/blob/main/lm.py#L224-L236", "venue": "Nature (2026)", "uses_safely": false, "mistake_ids": ["M4", "M6"], "impacted_findings": [{"finding": "Main results table (figures/full_table.tex): per-model accuracy/hallucination-rate matrix across 4 penalty thresholds (L=0,1,3,9) x {Closed, Open} rubric x {baseline, mitigated}, e.g. LLM2 (GPT) closed-rubric baseline at threshold 0% = 0.49, LLM4 (Opus) closed-rubric baseline at threshold 0% = 0.37, LLM1 (Gemini) open-rubric mitigated at threshold 90% = 0.88", "locator": "figures/full_table.tex (headline results table)", "mechanism": "M4 (no provenance logging) + M6 (version drift): lm.py's _generate() posts bare, unpinned model aliases ('google/gemini-3-pro-preview', 'openai/gpt-5', 'x-ai/grok-4', 'anthropic/claude-opus-4.5') with no `provider` dict anywhere in the repo (verified: zero hits for a routing/provider field in lm.py, the notebook, README, or EXAMPLES.md). The code path used throughout (output_mode='with_usage') actively discards the returned completion's `is_byok` and `cost_details` fields before storage, so even the metadata OpenRouter does return is thrown away -- there is no way after the fact to confirm which exact checkpoint/provider served any of the ~138k+ calls behind this table. Concretely: the README's own cost table names 'GPT-5.2' as the model queried, while lm.py's OPENROUTER_MODELS registry maps the 'gpt-5' key used by the notebook to the OpenRouter id 'openai/gpt-5' (not gpt-5.2) -- a documented code/doc mismatch that is itself an instance of M6.", "severity": "medium", "depends_on_or": "medium"}, {"finding": "Figure figures/simpleqa_halluc_rates.pdf: paired baseline-vs-consistency-mitigated hallucination-rate plot across all 4 models (LLM1-4)", "locator": "figures/simpleqa_halluc_rates.pdf (notebook cell 19, plot_paired_halluc_rates)", "mechanism": "Same as the main table (M4 no provenance + M6 alias/version drift): this figure is generated directly from the same unpinned, unlogged OpenRouter generate()/grade_answer() calls, so any mid-collection snapshot change behind a 'preview'-tagged or bare alias (e.g. gemini-3-pro-preview, or the gpt-5/gpt-5.2 discrepancy) would be silently baked into these points with no audit trail.", "severity": "medium", "depends_on_or": "medium"}, {"finding": "Figure figures/simpleqa_abstention.pdf: abstention-rate-vs-threshold curves with 95% bootstrap CIs, open-rubric (solid) vs closed-rubric baseline (dashed) for all 4 models", "locator": "figures/simpleqa_abstention.pdf (notebook cell 22)", "mechanism": "Same mechanism as above (M4 + M6): curves are built from the identical unpinned/unlogged OpenRouter LM.generate() and grade_answer() (GPT-4.1 judge) calls, so provenance of exactly which snapshot/provider produced each point is unverifiable post hoc.", "severity": "medium", "depends_on_or": "medium"}, {"finding": "GPT-5-mini vs o4-mini secondary check: closed/open-rubric (CR@0/OR@0) accuracy printed as 'redoing what is in the system cards to confirm', plus paired permutation-test p-values comparing the two models on OR@0 and CR@0", "locator": "notebook cells 33-34 (MINIS section)", "mechanism": "M4 (no provenance) + M6 (version drift), specifically acute for o4-mini: it is an older OpenAI model whose default OpenRouter alias route/backend could have shifted by the time this run executed, and -- as with the gpt-5/gpt-5.2 case above -- there is no logged snapshot id to confirm the OpenRouter-served checkpoint matches what OpenAI's own system card team benchmarked. Both models are OpenAI-proprietary so cross-provider quantization (M1) risk is low; the exposure is specifically an unverifiable model-identity match for this named 'confirm the system card' claim and the head-to-head p-value.", "severity": "low", "depends_on_or": "medium"}], "headline_impact": "The paper's headline qualitative claim (closed/accuracy-only grading drives higher hallucination rates than open-rubric grading, and consistency-mitigation reduces it) is unlikely to reverse, since it comes from within-model contrasts, not cross-provider comparisons; but the exact reported percentages in the main results table and figures cannot be verified as reproducible, because lm.py logs no provider/model-snapshot provenance for any OpenRouter call, and the repo's own README already shows a live version-label mismatch (cost table says \"GPT-5.2\" was queried; the code registry actually requests OpenRouter id \"openai/gpt-5\" for that same run).", "no_impact_reason": ""}, {"title": "Inspect AI (UK AI Safety Institute)", "repo_url": "https://github.com/UKGovernmentBEIS/inspect_ai", "paper_url": "", "code_url": "https://github.com/UKGovernmentBEIS/inspect_ai/blob/main/src/inspect_ai/model/_openai.py#L352-L363", "venue": "UK AISI (govt AI safety institute)", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M5", "M9"], "impacted_findings": [{"finding": "PersistBench `persistbench_cross_domain` failure_rate for `openrouter/meta-llama/llama-3.3-70b-instruct` = 25.0, outside the paper's 95% CI [12.0, 23.0]", "locator": "inspect_evals repo: src/inspect_evals/persistbench/README.md, 'Evaluation Report' table, row 1 (also 2026-05-22 archived .eval JSON results/persistbench_beneficial_memory/2026-05-22_openrouter_meta-llama_llama-3.3-70b-instruct.json for the sibling beneficial_memory run)", "mechanism": "M1 (no quantization pin) + M3 (probabilistic routing). Verified directly in the raw .eval log: eval.model='openrouter/meta-llama/llama-3.3-70b-instruct', eval.model_args={}, plan.config={} — i.e. zero provider/quantization constraints were sent. This traces to inspect_ai/model/_providers/openrouter.py: `self.provider = collect_model_arg('provider')` (L108-112) defaults to None and is only injected into `extra_body['provider']` inside `completion_params()` (L372-383) when the caller explicitly passes `-M provider={...}`. The reproduction command in the README (`inspect eval-set ... --model openrouter/meta-llama/llama-3.3-70b-instruct`) never does this, so OpenRouter was free to route the 70B open-weight model to any registered inference provider at any precision (int4/int8/fp8/bf16) it had capacity for, and to a different provider on a different run — a plausible source of the out-of-CI drift vs. the paper's presumably fixed inference stack.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Remaining 5 persistbench failure_rate figures for the same two open-weight subject models across the three tasks (e.g. sycophancy 85.0/99.0, beneficial_memory 54.0/22.0, cross_domain 53.0 for gpt-oss-120b)", "locator": "inspect_evals repo: src/inspect_evals/persistbench/README.md 'Evaluation Report' table (all 6 rows) and src/inspect_evals/persistbench/report.md", "mechanism": "M1 + M3, same as above — every subject-model row uses `openrouter/<org>/<open-weight-model>` with no `-M provider=...` model arg, so all six numbers were produced on the same unpinned default path. Several land inside the paper's CI on this particular run, but the underlying quantization/provider is not pinned or logged, so run-to-run reproducibility of any of these six numbers is not guaranteed (a re-run could silently land on a different backend/precision).", "severity": "medium", "depends_on_or": "high"}, {"finding": "Default PersistBench judge `openrouter/moonshotai/kimi-k2-thinking`, described as chosen because 'the paper's judge calibration was performed against Vertex AI-hosted Kimi K2 Thinking, ensuring score consistency with published results' — used to score all persistbench_* results", "locator": "inspect_evals repo: src/inspect_evals/persistbench/scorers.py (DEFAULT_JUDGE_MODEL, DEFAULT_JUDGE_PROVIDER) and README.md 'Scoring' section", "mechanism": "Partial mitigation, residual M2 risk. The provider *routing* is correctly pinned (`DEFAULT_JUDGE_PROVIDER = {'order': ['google-vertex'], 'allow_fallbacks': False}`), which avoids M3/M8 cross-backend drift for the judge itself. But `require_parameters` is not set and no `quantizations` constraint is given, so `GenerateConfig(temperature=0, reasoning_effort='high')` + `reasoning_enabled=True` sent to the judge could be silently dropped/altered by OpenRouter/Vertex rather than erroring if any combination is unsupported, undermining the stated 'score consistency with published results' guarantee for every judged score in this benchmark.", "severity": "low", "depends_on_or": "medium"}, {"finding": "The documented/idiomatic Inspect AI OpenRouter invocation pattern itself, `inspect eval <task> --model openrouter/<org>/<model>` (exact form used in Inspect's own quickstart example `inspect eval arc.py --model openrouter/gryphe/mythomax-l2-13b`), ships with no default quantization pin or require_parameters", "locator": "Inspect AI docs: providers.html 'OpenRouter' section (inspect.aisi.org.uk/providers.html#openrouter); code: src/inspect_ai/model/_providers/openrouter.py L95-135 (provider/models/transforms args default to None via collect_model_arg) and L334-385 (completion_params only adds extra_body['provider'] when self.provider is truthy)", "mechanism": "M1 + M2 + M3 systemically. Inspect's own docs state 'OpenRouter may distribute consecutive requests for the same model across multiple ... backends' and show pinning (`-M provider={...}`) only as an opt-in example, never as a default. This is the general class of downstream results instruction #4 asks about: any Inspect-run capability/safety eval, agent rollout, or LLM-judge score (across METR, Apollo Research, inspect_evals broadly, and published scheming/sandbagging/control papers) that specifies an open-weight model via `--model openrouter/<org>/<model>` without appending `-M provider='{\"quantizations\": [...], \"require_parameters\": true}'` inherits this exact unpinned path. The concrete persistbench findings above are one verified instantiation of this class; other inspect_evals tasks and third-party papers using the same idiomatic invocation are exposed identically but were not individually audited here.", "severity": "high", "depends_on_or": "high"}], "headline_impact": "In the UKGovernmentBEIS/inspect_evals PersistBench reproduction (run entirely through Inspect AI's default, unpinned OpenRouterAPI path), the measured cross-domain-leakage failure_rate for `openrouter/meta-llama/llama-3.3-70b-instruct` is 25.0, which falls OUTSIDE the PersistBench paper's reported 95% bootstrap CI of [12.0, 23.0] (arXiv:2602.01146) — a discrepancy plausibly explained by silent quantization/provider variance on the unpinned route.", "no_impact_reason": ""}, {"title": "MathArena (ETH Zurich SRI Lab)", "repo_url": "https://github.com/eth-sri/matharena", "paper_url": "", "code_url": "https://github.com/eth-sri/matharena/blob/main/src/matharena/api_client.py#L314-L319", "venue": "ETH Zurich SRI Lab (Martin Vechev group)", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M5", "M8"], "impacted_findings": [{"finding": "Kimi K2 leaderboard row (human_readable_id \"Kimi K2\", model moonshotai/kimi-k2)", "locator": "configs/models/moonshot/k2.yaml (api: openrouter, no provider/extra_body pin) — MathArena leaderboard entry \"Kimi K2\"", "mechanism": "M1 (unpinned quantization) + M3 (probabilistic routing): the config has no `extra_body.provider` field at all — unlike its own sibling configs k2-thinking.yaml, k25.yaml and k26.yaml, all added later, which pin `allow_fallbacks: False, order: [moonshotai]`. Kimi K2 (1T-param / 32B-active MoE) is hosted by many third-party OpenRouter providers at varying quantization; with no pin, OpenRouter's default load-balancer can silently serve this specific row from any of them, and can switch providers between runs of the same eval.", "severity": "high", "depends_on_or": "high"}, {"finding": "Llama-4-Maverick leaderboard row (human_readable_id \"Llama-4-Maverick\", model meta-llama/llama-4-maverick)", "locator": "configs/models/llama4/maverick.yaml (api: openrouter; top-level `provider: {allow_fallbacks: False, order: [Fireworks]}` key) — MathArena leaderboard entry \"Llama-4-Maverick\"", "mechanism": "M1 + M3, but with a code-verified twist: the maintainers clearly intended to pin this model to Fireworks (README even documents `provider` as a supported config key), but this config is the ONLY one of 8 pinned OpenRouter configs in the repo that puts `provider` at the YAML top level instead of nested under `extra_body:` (compare configs/models/moonshot/k2-thinking.yaml, k25.yaml, k26.yaml and the qwen3_*_2507/vl/30b configs, which all correctly nest it as `extra_body: {provider: {...}}`). Neither `_kwarg_remover` (api_client.py L223-241) nor `_clean_client_args` (math_agent.py L77-81, math_static_agent.py, selfcheck_agent.py) strip or relocate a bare `provider` key, so it survives into `self.kwargs` and is unpacked directly into `client.chat.completions.create(**payload)` (api_client.py ~L1745). I installed the repo's pinned minimum `openai>=1.102.0` and confirmed empirically that `Completions.create(provider={...})` raises `TypeError: Completions.create() got an unexpected keyword argument 'provider'` — this happens client-side, before any HTTP request. So the intended provider pin cannot function as written in the current code; the row is either produced by a different/older code path than what's on main today, or is running fully unpinned against OpenRouter's default fallback routing exactly as if no mitigation existed.", "severity": "high", "depends_on_or": "high"}, {"finding": "Phi-4-reasoning-plus leaderboard row (human_readable_id \"Phi-4-reasoning-plus\", model microsoft/phi-4-reasoning-plus:free)", "locator": "configs/models/phi4/14b-reasoning-plus.yaml (api: openrouter, model slug ends `:free`, no provider/extra_body pin) — MathArena leaderboard entry \"Phi-4-reasoning-plus\"", "mechanism": "M1 + M3: this config has zero provider pinning and explicitly targets OpenRouter's `:free` route, which routes across whichever community/rate-limited providers are currently offering the model for free — the segment of OpenRouter with the most run-to-run provider and quantization variance, and no cost signal to detect a downgrade.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Qwen3-235B-A22B leaderboard row, original 2025-04-29 entry (human_readable_id \"Qwen3-235B-A22B\", model qwen/qwen3-235b-a22b)", "locator": "configs/models/qwen/qwen3_235b_a22b.yaml (api: openrouter, no provider/extra_body pin) — MathArena leaderboard entry \"Qwen3-235B-A22B\"", "mechanism": "M1 + M3 directly, plus M8 (cross-provider comparison confound): this is the ONLY Qwen3 config in the repo without an `extra_body.provider` pin — the later configs qwen3_235b_a22b_2507.yaml, qwen3_vl_235b_a22b_thinking.yaml, qwen3_vl_235b_a22b_instruct.yaml, qwen3.5_397b.yaml and qwen3_30b_a3b.yaml all added the pin. The leaderboard ranks this unpinned row against its own pinned sibling releases and against proprietary single-served rows on nominally identical methodology, without controlling for the fact that only this row's inference backend/quantization is uncontrolled.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Project-wide: MathArena's core \"contamination-free\" premise for the same OpenRouter-unpinned rows (Kimi K2, base Qwen3-235B-A22B, Phi-4-reasoning-plus:free)", "locator": "Structural risk across configs/models/{moonshot/k2.yaml, qwen/qwen3_235b_a22b.yaml, phi4/14b-reasoning-plus.yaml} — no ZDR/provider control visible in api_client.py's openrouter branch (L314-319)", "mechanism": "M5 (data-policy leakage): MathArena's entire value proposition is evaluating on newly-released, not-yet-public competition problems to avoid training contamination. For rows with no provider pin, the (still-unpublished) problem text is sent as a prompt through whichever third-party OpenRouter-listed provider happens to serve the request, subject to that provider's own data-retention/training policy rather than a policy MathArena controls — a latent risk to the contamination-free guarantee for exactly the rows already flagged above, independent of any single reported score.", "severity": "low", "depends_on_or": "medium"}], "headline_impact": "MathArena's leaderboard rows for Kimi K2, Llama-4-Maverick, Phi-4-reasoning-plus, and (the original) Qwen3-235B-A22B are queried via OpenRouter with no functioning provider pin — for Llama-4-Maverick the pin that IS present in the YAML config is a code-verified no-op (it crashes the OpenAI SDK call rather than reaching OpenRouter), meaning these open-weight rows are exposed to unpinned/quantized/rotating-provider routing while being ranked head-to-head against proprietary and properly-pinned rows on the same public leaderboard.", "no_impact_reason": ""}, {"title": "OSWorld (NeurIPS 2024 D&B) — mm_agents/{maestro,os_symphony,vlaa_gui} OpenRouter engine", "repo_url": "https://github.com/xlang-ai/OSWorld", "paper_url": "https://arxiv.org/abs/2404.07972", "code_url": "https://github.com/xlang-ai/OSWorld/blob/main/mm_agents/maestro/core/model.md#L175-L209", "venue": "NeurIPS 2024 (Datasets and Benchmarks Track)", "uses_safely": false, "mistake_ids": ["M4", "M5", "M6"], "impacted_findings": [{"finding": "\"Agentic Lybic achieves a state-of-the-art 57.07% success rate in 50 steps\" on OSWorld, beating CoAct-1 (56.39%), Agent S2.5 w/ o3 (54.21%), Jedi-7B w/ o3 (50.65%)", "locator": "Table 1 / abstract, arXiv:2509.11067 (\"Agentic Lybic: Multi-Agent Execution System with Tiered Reasoning and Orchestration\"); the paper states \"Our implementation is publicly available at https://github.com/xlang-ai/OSWorld/tree/main/mm_agents/maestro to facilitate reproducibility\" — i.e. this is literally the audited code path. The same run also underlies the 'maestro' entry on the OSWorld project's own hosted leaderboard (osworld-v1.xlang.ai).", "mechanism": "Paper states 'For all core reasoning components (Controller, Manager, and the three Worker roles - Technician, Operator, and Analyst), we utilize OpenAI o3.' The shipped default config (mm_agents/maestro/tools/new_tools_config.json) routes exactly these roles (planner_role, supplement_role, dag_translator, operator_role, technician_role, analyst_role, worker_success_role, worker_stale_role, periodic_role, final_check_role — 10 of ~15 tool entries) through provider:\"openrouter\", model_name:\"openai/o3\". M6 (version drift): the slug is bare/undated, so the exact snapshot behind the reported number is unpinned and can silently change, contradicting the paper's stated reproducibility goal. M5 (data-policy leakage): LMMEngineOpenRouter (mm_agents/maestro/core/engine.py) never sets provider/data_collection, so OpenRouter's default 'allow' policy applies while full desktop screenshots are sent on every one of these calls during live task execution — undisclosed in the paper. M4 (no provenance logging): engine.generate() calls response.choices[0].message.content and calculate_tokens_and_cost(response,...) and returns only (content, total_tokens, cost) — the response object (including OpenRouter's actual-serving-provider field) is discarded, so neither the paper authors nor anyone attempting to reproduce 57.07% can verify which backend organization actually served the o3 calls behind it.", "severity": "high", "depends_on_or": "high"}, {"finding": "Downstream/class risk: any OSWorld success-rate result produced by running mm_agents/os_symphony or mm_agents/vlaa_gui with engine_type=\"open_router\" (both ship a near-identical LMMEngineOpenRouter class copied from the same pattern) inherits the identical M4/M5/M6 exposure. This is distinct from — and should not be confused with — the two agents' own currently-published headline numbers: OS-Symphony's 65.84% (Table 1, arXiv:2601.07779, backbone \"GPT-5\") and VLAA-GUI's 77.45%/74.89% (arXiv:2604.21375, backbones Claude Opus 4.6/4.5), whose methods sections name only direct OpenAI/Anthropic/Google/ByteDance API access and never mention OpenRouter.", "locator": "mm_agents/os_symphony/core/engine.py and mm_agents/vlaa_gui/core/engine.py (ENGINE_REGISTRY entries \"open_router\"/\"anthropic_bedrock\"/etc.); contrast with OS-Symphony Table 1 (arXiv:2601.07779) and VLAA-GUI Table 2 (arXiv:2604.21375)", "mechanism": "Same M4/M5/M6 pattern as the maestro finding above would apply to anyone who selects the OpenRouter engine option for these agents, but as published the two papers' own headline claims do not appear to route through OpenRouter (VLAA-GUI's own README even states its default main reasoning model is direct \"Anthropic Bedrock\"), so this is a latent/inherited risk for future reproductions and leaderboard submissions of these two agents rather than a confirmed distortion of an already-reported number.", "severity": "low", "depends_on_or": "low"}], "headline_impact": "Agentic Lybic's (a.k.a. \"maestro\", mm_agents/maestro) published SOTA claim of 57.07% success rate on OSWorld (50-step, Table 1 of arXiv:2509.11067) is produced by routing all five core reasoning roles (Controller/Manager/Technician/Operator/Analyst) through OpenRouter via the bare, undated slug \"openai/o3\" — the paper explicitly cites this exact repo path as its reproducibility artifact, and the shipped default config leaves data_collection at OpenRouter's \"allow\" default while discarding all provider/response metadata.", "no_impact_reason": ""}, {"title": "OpenHands (formerly OpenDevin)", "repo_url": "https://github.com/OpenHands/OpenHands", "paper_url": "https://arxiv.org/abs/2407.16741", "code_url": "https://github.com/OpenHands/OpenHands/blob/main/pyproject.toml", "venue": "arXiv (other)", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M4", "M5", "M6", "M8", "M10"], "impacted_findings": [{"finding": "Downstream SWE-bench Verified / GAIA / WebArena leaderboard and model-release-blog claims of the form 'OpenHands + <open-weight model> resolved X%' where the model was served through OpenRouter (e.g. DeepSeek-V3/R1, Qwen2.5/3-Coder, Kimi K2, GLM-4.5/4.6, Llama-3.1-405B) — not a number appearing in this paper's own Tables 3-6, but the class of results that inherit OpenHands' default OpenRouter configuration", "locator": "OpenHands evaluation harness (evaluation/benchmarks/{swe_bench,gaia,webarena} in github.com/OpenHands/OpenHands) run with the documented OpenRouter provider path (docs page openhands/usage/llms/openrouter.mdx); manifests externally as SWE-bench/GAIA leaderboard entries and vendor blog posts citing 'OpenHands' as scaffold, not inside arXiv:2407.16741 itself", "mechanism": "M1 (no quantizations pin — OpenRouter may silently serve an int4/int8 build of the open-weight model instead of full precision), M2 (litellm_extra_body defaults to {} so temperature/top_p/seed set by the eval harness are not enforced via require_parameters and can be silently dropped by whichever provider is picked), M3 (no order/only/sort, so the serving provider — and therefore latency, quantization, and quality — can change between runs or even mid-run), and M4 (no provenance logging, so a reported score can't be traced to which specific backend/quant actually produced it). Confirmed in code: the OpenRouter docs page (openhands/usage/llms/openrouter.mdx) gives zero guidance on any of this, and the SDK's LLM class (openhands-sdk/openhands/sdk/llm/llm.py) exposes only openrouter_site_url/openrouter_app_name (cosmetic HTTP-Referer/X-Title headers) plus the empty-by-default litellm_extra_body as the sole routing lever.", "severity": "medium", "depends_on_or": "medium"}], "headline_impact": "None of the paper's own reported numbers (Tables 3-6, arXiv:2407.16741) are produced via OpenRouter — every OpenHands-run result uses direct OpenAI/Anthropic API models (gpt-4o, gpt-4o-mini, gpt-3.5-turbo, gpt-4-turbo, claude-3-5-sonnet-20240620, claude-3-opus); the OpenRouter risk instead sits entirely downstream, in the class of community SWE-bench/GAIA/WebArena leaderboard submissions that run the OpenHands harness with 'LLM Provider = OpenRouter' for open-weight models, a fully-documented but zero-safeguard path.", "no_impact_reason": ""}, {"title": "Prometheus 2 / BiGGen-Bench (KAIST AI)", "repo_url": "https://github.com/prometheus-eval/prometheus-eval", "paper_url": "https://arxiv.org/abs/2406.05761", "code_url": "https://github.com/prometheus-eval/prometheus-eval/blob/main/BiGGen-Bench/run_response_eval.py#L124-L125", "venue": "NAACL 2025 (BiGGen-Bench); EMNLP 2024 Findings (Prometheus 2, arXiv 2405.01535)", "uses_safely": false, "mistake_ids": ["M4", "M5", "M6", "M10"], "impacted_findings": [{"finding": "\"Performance Gap between Open-source Chat and Proprietary LMs Across Capabilities\" — Hedges's g effect sizes: Average 0.51, Instruction Following 0.38, Grounding 0.49, Planning 0.58, Reasoning 0.65, Refinement 0.46, Safety 0.36, Theory of Mind 0.59, Tool Usage 0.58, Multilingual 0.84 (condensed version appears as main-text Table 4)", "locator": "Table 11 (Appendix D.3) / Table 4 (main text, Section 4)", "mechanism": "M8 (cross-provider comparison confound) + M2 (silent param dropping): the proprietary-side candidate responses feeding this table are generated by BiGGen-Bench/run_api_inference.py via prometheus_eval's AsyncLiteLLM wrapper, and the repo README states verbatim 'we mainly used OpenAI API and OpenRouter' for this exact script — while the open-source side is run locally via vLLM at fixed precision. Independently confirmed by reading prometheus_eval/litellm.py: AsyncLiteLLM's async completion path (`_get_completion_text_async`) accepts `**kwargs` but never forwards it to litellm's `acompletion()` call — only model/api_base/messages/timeout are passed — so the temperature=1.0/top_p=0.9/max_tokens=2048 the paper intends for API models is silently dropped client-side before OpenRouter even sees the request, meaning the two arms of this comparison were never run under matched, controlled sampling conditions. Any OpenRouter-side quantization or provider switching on top of that directly inflates or deflates the proprietary arm's scores, biasing every effect size in this table.", "severity": "high", "depends_on_or": "high"}, {"finding": "\"Top 1 to 5 LMs on each capability scored by GPT-4-Turbo-2024-04-09\" — leaderboard rankings, e.g. Average: GPT-4-1106 > GPT-4-Turbo-0125 > GPT-4o-2024-05-13 > GPT-4-Turbo-2024-04-09 > Claude-3-Opus; Reasoning top-5 includes Gemini-Pro-1.5; Safety top-2: Claude-3-Opus, GPT-4-1106", "locator": "Table 6 (main text, Section 4)", "mechanism": "M8 + M3 (probabilistic routing / run-to-run provider switching): nearly every top-5 slot in most capability columns is filled by closely-clustered proprietary models (GPT-4 variants, Claude-3-Opus/Sonnet, Gemini-Pro-1.5) whose candidate responses were generated through the same OpenRouter/OpenAI-API path as Finding 1. Litellm/OpenRouter can route nominally-identical model calls to different backend providers or quantization levels between runs, and fine-grained ordinal rankings among tightly-scored proprietary models are exactly the kind of claim such routing noise can flip (e.g. which GPT-4 snapshot ranks #1 overall).", "severity": "medium", "depends_on_or": "high"}, {"finding": "Every score produced by the three proprietary evaluator LMs — Table 3 Pearson correlations with human judgment (Claude-3-Opus 0.624, GPT-4-1106 0.641, GPT-4-Turbo-2024-04-09 0.647, Majority Voting 0.627) and, since GPT-4-Turbo-2024-04-09 is used as the sole evaluator for the paper's primary scaling analyses, essentially all of Figure 4, Figure 5, Table 1, Table 2, and the GLM results in Tables 8-10", "locator": "Table 3 (Section 5.1) plus Figures 4-5 and Tables 1-2 (Section 4), which all use GPT-4-Turbo-2024-04-09 as evaluator LM", "mechanism": "M9 (judge on unconstrained route): the exact call site is BiGGen-Bench/run_response_eval.py lines 124-125 (`model = MockLLM(mode=\"absolute\"); # model = AsyncLiteLLM(eval_model_name, batch_size=100, requests_per_minute=100)`) — confirmed to match the cited primary OpenRouter call site; the released repo mocks this out publicly but it is the documented replacement used to actually produce the paper's judgments. Score extraction relies on parsing a free-text '[RESULT] N' suffix with no structured-output constraint, so any provider variation in instruction-following behavior shifts scores. Appendix G.1 explicitly claims 'we use hyperparameters of temperature 0.0 ... to ensure reproducibility for both the evaluator language model and the language model being evaluated,' but AsyncLiteLLM's async path never forwards sampling kwargs to acompletion() at all (same bug as Finding 1), so this reproducibility claim was likely not actually enforced for the API-routed judges — leaving the judge calls exposed to whatever decoding defaults and provider/quantization OpenRouter (or the underlying API) chose. Because GPT-4-Turbo-2024-04-09 alone is the evaluator behind nearly every headline figure/table in Sections 4-5, this is the single largest-blast-radius risk, but confidence it specifically routed through OpenRouter (vs. direct OpenAI/Anthropic keys) is only medium: the README's explicit OpenRouter statement is scoped to the response-generation script, not this evaluation script, and the call is mocked/commented out in the public code so the exact production configuration cannot be directly verified (this itself is the M4 no-provenance-logging gap already identified for the project).", "severity": "high", "depends_on_or": "medium"}], "headline_impact": "Table 4/Table 11's headline \"open-source vs. proprietary LM\" performance gap (Hedges's g = 0.51 average, ranging 0.36–0.84 by capability) rests on comparing proprietary-model responses generated via litellm/OpenRouter against open-weight responses run locally under controlled vLLM decoding — an apples-to-oranges comparison if OpenRouter silently altered quantization, provider, or sampling params on the proprietary side.", "no_impact_reason": ""}, {"title": "openbench (Groq)", "repo_url": "https://github.com/groq/openbench", "paper_url": "", "code_url": "https://github.com/groq/openbench/blob/main/src/openbench/model/_providers/openrouter.py#L57-L115", "venue": "Industry (Groq, official org repo)", "uses_safely": false, "mistake_ids": ["M1", "M2", "M3", "M5", "M7"], "impacted_findings": [{"finding": "The official OpenRouter usage example in openbench's own docs: `bench eval mmlu --model openrouter/openai/gpt-oss-120b --max-connections 100 -M only=groq,together -M sort=price` (docs/providers.mdx, 'OpenRouter Configuration Options' section)", "locator": "docs/providers.mdx, section 'OpenRouter' (code block after the parameter table)", "mechanism": "M1: no `-M quantizations=` is set, so OpenRouter can silently serve gpt-oss-120b (an open-weight MoE model) at any quantization level from either backend down to int4. M3: `only=groq,together` still leaves probabilistic selection between two backends/engines on every request — the same command can silently switch which provider (and therefore which quantization/engine) answered each question, and re-running it later is not guaranteed to reproduce the same routing. M3/cost-bias: `sort=price` explicitly asks OpenRouter to prefer the cheapest available route, which correlates with more aggressively quantized/degraded serving. This is the flagship copy-pasteable example for OpenRouter in the repo's own documentation, so any MMLU (or other benchmark, since only the task name changes) score produced by following it verbatim inherits all three issues.", "severity": "high", "depends_on_or": "high"}, {"finding": "The docs' secondary OpenRouter 'Tip' example: `bench eval simpleqa --model openrouter/openai/gpt-oss-120b -M only=groq` (docs/providers.mdx, Tip callout)", "locator": "docs/providers.mdx, `<Tip>` box under 'General Model Configuration'", "mechanism": "M1: still no `quantizations=` pin — OpenRouter's Groq-backed listing for gpt-oss-120b can still change quantization/serving parameters over time without the caller knowing (M6/M1 overlap). M2: `require_parameters` is left unset (defaults to None/False-equivalent in the wrapper), so if the OpenRouter route silently drops sampling params (temperature/top_p/seed/reasoning_effort) that SimpleQA's answer-extraction/grading logic depends on, the call succeeds with silently altered behavior rather than erroring. Restricting to `only=groq` does meaningfully reduce M3 cross-provider switching risk relative to Finding 1, hence lower severity here.", "severity": "medium", "depends_on_or": "high"}, {"finding": "The general class of downstream results across ALL 95+ openbench suites (explicitly including GPQA Diamond, AIME 2023-2025, MATH, MathVista, MathArena, MMLU) reported by any user who follows openbench's own 'Speedrun' invocation pattern (`bench eval <task> --model openrouter/<open-weight-model> --limit N`) but substitutes `openrouter/` for the README's `groq/` example, adding zero `-M` flags — the naive default path the README's own quickstart pattern invites", "locator": "src/openbench/model/_providers/openrouter.py L57-115 (constructor defaults: `only=None, order=None, allow_fallbacks=None, ignore=None, sort=None, max_price=None, quantizations=None, require_parameters=None, data_collection=None`) combined with README.md 'Speedrun' section and the CLI default-options table (`--seed` default `None`, `--temperature 0.6`)", "mechanism": "With every provider_params key left None, `self._extra_body` is empty and NO provider-routing constraints are injected into the request at all — full unconstrained OpenRouter default routing applies. M1: quantization completely unpinned for whichever open-weight model is named. M2: no `require_parameters=True`, so any sampling/format param the randomly-selected backend doesn't support is silently dropped rather than causing an error. M3: with `order`/`only`/`sort` all unset, OpenRouter load-balances across all eligible providers per-request, so a single benchmark run can mix quantizations/engines across samples within the same eval, and the run is not reproducible run-to-run. M5: `data_collection` unset means OpenRouter's default data-retention policy applies to eval prompts (including competition-math questions from AIME/MathArena, which are contamination-sensitive) unless the user separately knows to pass `-M data_collection=deny`. M7: openbench exposes a documented `--seed`/`BENCH_SEED` flag implying deterministic generation is achievable, but seed pinning does not confer determinism across OpenRouter's multi-provider/multi-engine routing — a user who sets `--seed` for reproducibility on an `openrouter/` model gets a false sense of reproducibility.", "severity": "high", "depends_on_or": "high"}], "headline_impact": "Any benchmark score (MMLU, SimpleQA, GPQA Diamond, AIME, MATH, MathVista, MathArena, etc.) that a downstream user reports from openbench's OWN documented OpenRouter invocations is at risk, because the OpenRouterAPI wrapper (src/openbench/model/_providers/openrouter.py L57-115) leaves quantization, parameter-enforcement, and data-collection controls unset (None) unless the user manually adds `-M` flags — and openbench's own docs examples never demonstrate the fully-safe combination.", "no_impact_reason": ""}, {"title": "tau2-bench (Sierra AI)", "repo_url": "https://github.com/sierra-research/tau2-bench", "paper_url": "https://arxiv.org/abs/2506.07982", "code_url": "https://github.com/sierra-research/tau2-bench/blob/main/src/tau2/knowledge/embedders/openrouter_embedder.py#L117-L118", "venue": "arXiv (Sierra AI); tau-bench family widely cited industry-wide for function-calling/agentic-LLM eval", "uses_safely": false, "mistake_ids": ["M1", "M3", "M4", "M5"], "impacted_findings": [{"finding": "GPT-5.2 (reasoning_effort: none) banking_knowledge leaderboard scores: pass^1=12.63, pass^2=7.22, pass^3=5.15, pass^4=4.12 (submission gpt-5-2-none_sierra_2026-02-26/submission.json, notes: \"Banking domain evaluated with retrieval_config: qwen_embeddings\")", "locator": "taubench.com leaderboard, banking_knowledge domain -> web/leaderboard/public/submissions/gpt-5-2-none_sierra_2026-02-26/submission.json in sierra-research/tau2-bench", "mechanism": "M1+M3: this row's retrieval_config is 'qwen_embeddings', which per src/tau2/knowledge/embeddings_cache.py resolves to backend='openrouter', model='qwen3-embedding-8b'. Every KB_search dense-retrieval embedding call for every task in this run went through OpenRouterEmbedder.embeddings.create() at openrouter_embedder.py:146, which passes only input/model/encoding_format -- no extra_body={'provider':...} filter and no provider.quantizations pin anywhere in the repo (confirmed zero grep hits for 'quantizations'). Qwen3-Embedding-8B is open-weight and load-balanced across OpenRouter providers by default, so embeddings for this eval run could silently have come from an int4/int8-quantized replica, and a re-run could land on a different provider entirely -- both change the embedding vectors used for cosine retrieval in KB_search_dense, which directly gates whether the agent finds the correct banking KB passage and therefore whether each task scores pass/fail. Because banking_knowledge is a low-absolute-score domain (12.63% pass^1), retrieval-vector drift from provider/quantization variance is proportionally large relative to the reported number.", "severity": "high", "depends_on_or": "high"}, {"finding": "Within-model comparison on the taubench.com leaderboard: GPT-5.2 default (reasoning_effort=high, retrieval_config='alltools'/OpenAI embeddings) banking_knowledge pass^1=32.22 vs GPT-5.2 reasoning_effort=none (retrieval_config='qwen_embeddings'/OpenRouter) banking_knowledge pass^1=12.63", "locator": "taubench.com leaderboard, banking_knowledge domain -> compare web/leaderboard/public/submissions/gpt-5-2_sierra_2026-02-26/submission.json (alltools, pass_1=32.22) vs gpt-5-2-none_sierra_2026-02-26/submission.json (qwen_embeddings, pass_1=12.63)", "mechanism": "M8: these two leaderboard rows for the same underlying model (GPT-5.2) differ in two variables at once -- reasoning_effort (high vs none) AND the KB embedding backend/provider (proprietary OpenAI text-embedding-3-large vs open-weight qwen3-embedding-8b served through OpenRouter's unpinned multi-provider routing). The leaderboard presents the ~2.5x score gap as attributable to reasoning effort, but part of it is confounded by the embedding-provider swap -- a viewer cannot cleanly attribute the delta to reasoning_effort alone because the retrieval backend (and its OR-induced quantization/provider variance per M1/M3) changed simultaneously.", "severity": "medium", "depends_on_or": "high"}, {"finding": "Downstream class of results: any tau2-bench banking_knowledge evaluation (leaderboard submission or private eval) run with retrieval_config in {qwen_embeddings, qwen_embeddings_grep, qwen_embeddings_reranker, qwen_embeddings_reranker_grep, alltools-qwen} inherits OpenRouter-routed dense retrieval by construction", "locator": "src/tau2/knowledge/embeddings_cache.py (backend='openrouter' mapping for all *-qwen configs); docs/leaderboard-submission.md explicitly documents `tau2 run --domain banking_knowledge --retrieval-config alltools-qwen ...` as a submission example", "mechanism": "M1+M3+M4: the benchmark's own docs actively steer submitters toward the OR-routed qwen configs as one of two standard embedding options (the other being OpenAI). Any external group that follows this documented path and reports banking_knowledge numbers (on taubench.com or in their own writeup) is exposed to the same unpinned-quantization/probabilistic-provider-routing risk as finding 1, and because openrouter_embedder.py logs no provider/quantization metadata per call (M4), no submitter using these configs can retroactively prove which provider or precision actually served their embeddings -- so any qwen_embeddings-family banking_knowledge number on the leaderboard is unverifiable and non-reproducible on re-run.", "severity": "medium", "depends_on_or": "high"}], "headline_impact": "The arXiv paper (2506.07982, telecom/retail/airline only) reports zero OpenRouter-touched numbers, but on the live taubench.com leaderboard the GPT-5.2 (reasoning_effort=none) banking_knowledge row — pass^1=12.63, pass^2=7.22, pass^3=5.15, pass^4=4.12 — was produced with retrieval_config=\"qwen_embeddings\", which routes every KB_search dense-retrieval call through OpenRouterEmbedder to qwen3-embedding-8b with no quantization pin and no provider filter.", "no_impact_reason": ""}, {"title": "Palisade Research — robot_shutdown_resistance", "repo_url": "https://github.com/PalisadeResearch/robot_shutdown_resistance", "paper_url": "https://github.com/PalisadeResearch/robot_shutdown_resistance/blob/main/paper-typst/main.pdf", "code_url": "https://github.com/PalisadeResearch/robot_shutdown_resistance/blob/main/src/live-experiments/llm_control/experiment/controller.py#L343-L349", "venue": "other (Palisade Research technical report, 2026-02-11)", "uses_safely": true, "mistake_ids": [], "impacted_findings": [], "headline_impact": "None — the paper's headline numbers (3/10 live-robot trials, 52/100 default-prompt simulated trials, 2/100 allow-shutdown simulated trials, all in the \"Results\" section and Figure 2) are produced by src/live-experiments/llm_control/experiment/controller.py, which calls `litellm.completion(model=\"xai/grok-4-0709\", ...)` — a direct LiteLLM→xAI API call, not an OpenRouter-routed call.", "no_impact_reason": "Verified directly: (1) Downloaded and read the full 9-page paper (main.pdf) — it names the tested model as \"Grok 4 v0709\" (footnote 2: chosen because it showed the highest shutdown-resistance rate in the prior Schlatter et al. work) and never mentions OpenRouter anywhere in the text. (2) Fetched controller.py in full — the sole LLM call site (`_call_llm`, around the L343 region cited in the task) is `litellm.completion(model=self._model, ...)` with `DEFAULT_MODEL = \"xai/grok-4-0709\"`; LiteLLM routes the \"xai/\" prefix directly to xAI's own API (XAI_API_KEY), not through OpenRouter. (3) The live-experiments README confirms supported keys are XAI_API_KEY / OPENAI_API_KEY / ANTHROPIC_API_KEY, with no OpenRouter mention, and Grok 4 is a proprietary, single-provider-served (xAI) model, not open-weight — contrary to the task's supplied \"model type: open_weight\" tag, which appears to be a metadata error; this makes M1 (quantization drift) essentially inapplicable regardless of routing. (4) `gh search code` across the whole repo confirms every OpenRouter reference (README example command `--model openrouter/x-ai/grok-4`, a cost-computation script hitting openrouter.ai/api/v1/models, and commented-out OPENROUTER_API_KEY placeholders) lives only in src/initial-experiments, whose own README states up front it is superseded by src/live-experiments — i.e., off the path that produced any number reported in the paper. Note, unrelated to OpenRouter: src/live-experiments/llm_control/.env in the repo appears to contain live-looking OpenAI, xAI, AWS, and Langfuse credentials committed in plaintext — worth flagging to the maintainers/Matthew as a separate security issue, though out of scope for this OpenRouter-routing assessment."}, {"title": "R1 CoT Illegibility Revisited (nostalgebraist, fork of Jozdien/cot_legibility)", "repo_url": "https://github.com/nostalgebraist/cot_legibility", "paper_url": "https://www.lesswrong.com/posts/jHnZzicKzczkCCArK/r1-cot-illegibility-revisited", "code_url": "https://github.com/nostalgebraist/cot_legibility/blob/nost/src/inference/providers.py#L37-L41", "venue": "LessWrong", "uses_safely": true, "mistake_ids": [], "impacted_findings": [], "headline_impact": "None — the headline Novita-vs-Targon comparison (illegibility 2.28 vs 4.30; %>=5 illegible 0.5% vs 34.9%; correctness 40.5% vs 36.6%) is not distorted by OpenRouter routing, because the repo pins provider with allow_fallbacks:false and logs per-response provider confirmation showing 100% Novita on every one of the 198 sampled responses.", "no_impact_reason": "I verified the actual code and run artifacts, not just the post text, and every plausible mistake vector is closed off for the results this post reports:\n\n1. M3 (probabilistic routing) / M11 (silent backend mixing) — closed. `src/inference/providers.py` (OpenRouterProvider.generate) builds `extra_body[\"provider\"] = {\"only\": [provider_config], \"allow_fallbacks\": False}` whenever `openrouter_provider` is set in the model config. The config actually used for the headline numbers, `config/2026_04_19__r1__all__novita_only.yaml` (and the matching `runs/20260419_100716_R1_gpqa/config.yaml`, `runs/20260419_103551_R1_gpqa/config.yaml`), sets `openrouter_provider: [novita]`. I pulled the raw `inference.json` for the 200-question run and checked every record's logged `metadata.openrouter_provider` and `metadata.provider_model`: all 198 non-empty responses show `\"openrouter_provider\": \"Novita\"`, `\"provider_model\": \"deepseek/deepseek-r1\"` — no run-to-run provider drift, no mixing.\n\n2. M4 (no provenance logging) — closed. The provider code explicitly captures `last_chunk.provider` and `last_chunk.model` off the OpenRouter streaming response into every stored record (`result[\"openrouter_provider\"]`, `result[\"provider_model\"]`), so each of the reported data points carries its own served-provider confirmation — this is exactly the provenance discipline the mistake category is about, already implemented.\n\n3. M1 (unpinned quantization) — largely mitigated for the load-bearing comparison. The post itself states both Targon and Novita were confirmed running fp8, i.e., the author checked for a quantization confound between the two conditions being compared rather than assuming it away. Residual risk is low-to-negligible: pinning to a single named provider (Novita) rather than a `provider/quant` string doesn't cryptographically guarantee quantization never changes server-side, but this is a minor residual, not a finding that plausibly reverses or meaningfully shifts the reported gap (0.5% vs 34.9% illegible-≥5, 2.28 vs 4.30 mean illegibility) — the effect size is far too large to be a quantization artifact.\n\n4. M9 (judge on unconstrained route) — not applicable. The grader model for the reported runs is `gpt-4o` (`evaluation.grader_model: gpt-4o` in the run configs), and `src/utils/models.py`'s `MODEL_REGISTRY` maps `\"gpt-4o\"` to `{\"provider\": \"openai\", ...}`, which `get_provider()` routes to `DirectAPIProvider(\"openai\")` — a direct `OPENAI_API_KEY` OpenAI SDK call, never touching `openrouter.ai`. So the illegibility/correctness judge scores that produce the headline numbers are not OpenRouter-routed at all.\n\n5. M8 (cross-provider comparison confound) — not applicable as a \"mistake\": the Targon-vs-Novita comparison is the intentional object of study, not an accidental confound smuggled into an unrelated result. The Targon baseline numbers are pulled from the original paper's archived rollouts (via `scripts/convert_archived_rollouts.py`, feeding `runs/2025101*_R1_gpqa` analysis-only runs with no `inference` stage), not re-generated by this repo's own uncontrolled OpenRouter calls.\n\n6. M2/M6/M7 (param dropping, version drift, seed) — no evidence of impact: `temperature: 1.0` and `include_reasoning: true` are explicitly set and passed through every call; no `max_tokens` cap is set for the R1 runs (avoiding the truncation problem the post itself flags for Azure, which is excluded from the reported comparison for exactly that reason).\n\nGiven the explicit provider pin with `allow_fallbacks: False`, the empirically-verified 100% single-provider provenance log across every sampled response, and a judge model that bypasses OpenRouter entirely, none of the reported findings (the illegibility means, the ≥5-threshold percentages, or the correctness-rate breakdown) plausibly route through an OpenRouter mistake pathway. This is consistent with the pre-assigned SAFE verdict.</no_impact_reason>\n"}];

const IMPACT_SCHEMA = {
  type: 'object', additionalProperties: false,
  properties: {
    impacted_findings: { type: 'array', items: { type: 'object', additionalProperties: false,
      properties: {
        finding: { type: 'string' }, locator: { type: 'string' }, mechanism: { type: 'string' },
        severity: { type: 'string', enum: ['high','medium','low'] },
        depends_on_or: { type: 'string', enum: ['high','medium','low'] },
      }, required: ['finding','locator','mechanism','severity','depends_on_or'] } },
    headline_impact: { type: 'string' },
    no_impact_reason: { type: 'string' },
  },
  required: ['impacted_findings','headline_impact'],
}

const VERIFY = (r) => `
Adversarially verify this "possibly-impacted findings" list for ${r.title}. Your job: strip
OVERCLAIMS and tighten. These findings came from a FIRST-PASS agent that has been observed to
invent plausible-looking numbers, so re-check every cited figure against the real source.

repo: ${r.repo_url}
paper/report: ${r.paper_url || '(none - use the README / linked report)'}
primary OpenRouter call site: ${r.code_url || r.repo_url}
our safety verdict: ${r.uses_safely ? 'SAFE' : 'AT RISK'} | mistakes: ${(r.mistake_ids||[]).join(', ')}

CANDIDATE LIST (JSON):
${JSON.stringify({impacted_findings: r.impacted_findings, headline_impact: r.headline_impact, no_impact_reason: r.no_impact_reason}, null, 2)}

CHECK each finding:
- Does the cited number/figure ACTUALLY EXIST in the paper/repo? Quote what you find. If it does
  not exist, DROP it (or replace it with the real number and say so).
- Does the result actually depend on OpenRouter-routed calls? (direct first-party API, local model,
  or proprietary single-served model with no quantization spread => downgrade depends_on_or or drop.)
- Is the mechanism the right M-id? Is severity honest?
Keep only findings that survive; you may ADD a clearly-missed one. Rewrite headline_impact to the
single most defensible impacted claim (or "none").

Use Bash ('gh api', 'git clone --depth 1'), WebFetch (paper/raw.githubusercontent) and WebSearch.
Do NOT spawn sub-agents. Return ONLY the schema object.
`

phase('Verify')
const out = await parallel(PENDING.map(r => () =>
  agent(VERIFY(r), { label: `verify:${(r.title||'').slice(0,36)}`, phase: 'Verify',
    model: 'sonnet', agentType: 'general-purpose', schema: IMPACT_SCHEMA })
    .then(v => v ? ({
      title: r.title, repo_url: r.repo_url,
      impacted_findings: v.impacted_findings || [],
      headline_impact: v.headline_impact || '',
      no_impact_reason: v.no_impact_reason || '',
      verified: true,
    }) : null)
))
const ok = out.filter(Boolean)
log(`verified ${ok.length}/${PENDING.length} pending rows`)
return { rows: ok }
